= provide(:title, "Sign up")
%h1 Sign up

.row
    .col-md-6.col-md-offset-3
        = form_for(@user) do |f|
            = render "fields", f: f
            = f.submit "Create my account", class: "btn btn-primary"
        %p
            Already have an account?
            %a= link_to "Log in now!", login_path